# hooks/

Custom React hooks (e.g. `useAuth`, `useCourses`) that wrap `lib/api-client.ts`
calls with React state/caching. Empty as of Module 2 — populated starting
Module 5/6 alongside the first real UI that needs them.
