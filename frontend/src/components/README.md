# components/

Reusable UI components (buttons, cards, layout primitives, form controls).

Empty as of Module 2 (frontend project initialization only) — populated
starting Module 5 (Teacher Dashboard) per docs/05-roadmap-and-milestones.md.
When this module starts building real components, see
`/mnt/skills/public/frontend-design` conventions for styling approach.
